# 自动化求职助手系统 — 完整实施方案

---

## 第一步：三个项目分析

### 1.1 n8n — 自动化调度中枢

| 维度 | 详情 |
|------|------|
| **核心功能** | 工作流自动化平台，400+ 集成节点，支持定时触发、HTTP 请求、数据转换、条件分支等 |
| **角色定位** | **调度中枢 + 数据抓取引擎**：负责定时触发抓取任务、调用外部 API、串联各模块、记录日志 |
| **对外接口** | REST API（`/api/v1/`，Bearer Token 认证）、Webhook（`/webhook/`）、内部 API（`/rest/`） |
| **部署方式** | Docker（推荐）、npm（`npx n8n`） |
| **数据传递** | 工作流中通过 JSON 数据在节点间流转，支持 HTTP Request 节点调用外部服务 |

**关键能力**：
- **Schedule Trigger**：Cron 表达式定时触发（如每 6 小时一次）
- **HTTP Request**：调用 Boss 直聘搜索接口、调用 career-ops 评分 API
- **Code（JavaScript）**：在工作流中做数据清洗、JSON 解析
- **Webhook**：接收 geekgeekrun 的投递结果回调
- **Postgres/SQLite 节点**：直接读写数据库
- **IF/Switch**：条件判断（分数 > 80 则投递，否则跳过）

---

### 1.2 career-ops — AI 评分引擎

| 维度 | 详情 |
|------|------|
| **核心功能** | AI 驱动的职位评估管线：扫描门户 → 过滤去重 → AI 评估打分 → 生成简历 PDF → 追踪记录 |
| **角色定位** | **智能筛选 + 评分模块**：接收 JD 文本，输出匹配度分数（0-100 或 1-5 分制），提供详细评估报告 |
| **对外接口** | 命令行（`node gemini-eval.mjs "JD文本"`）、`node scan.mjs` 扫描门户（无 REST API，需封装） |
| **部署方式** | Node.js（`npm install` + 环境变量配置） |
| **数据传递** | 输入：JD 文本或文件；输出：控制台文本 + `reports/` 目录下的 Markdown 报告 + `data/applications.md` 追踪器 |

**关键脚本**：
- **`scan.mjs`**：零 Token 消耗的门户扫描器，支持 Greenhouse、Ashby、Lever 等 ATS 平台
- **`gemini-eval.mjs`**：使用 Gemini API 对 JD 进行 A-G 七模块评估，输出 1-5 分评分
- **评分体系**：4.5+ = 强匹配，4.0-4.4 = 好匹配，3.5-3.9 = 一般，<3.5 = 不建议

**局限性**：
- 原生不提供 REST API，需要封装为 HTTP 服务供 n8n 调用
- 主要面向海外 ATS 平台（Greenhouse/Ashby/Lever），不直接支持 Boss 直聘等国内平台
- 评分 prompt 需要根据嵌入式/Linux 开发岗位进行定制

---

### 1.3 geekgeekrun — Boss 直聘自动投递

| 维度 | 详情 |
|------|------|
| **核心功能** | 基于 Electron + Puppeteer 的 Boss 直聘自动批量开聊工具，支持自动筛选、标记不合适、已读不回复聊 |
| **角色定位** | **自动投递模块**：在 Boss 直聘上自动与匹配的 Boss 开聊（即"投递"） |
| **对外接口** | Electron CLI 模式（`--mode=geekAutoStartWithBoss`）、出站 Webhook（将结果发送到外部）、无入站 REST API |
| **部署方式** | Electron 桌面应用（Windows/macOS/Linux），需 GUI 环境（Puppeteer 默认 `headless: false`） |
| **数据传递** | 配置文件（`~/.geekgeekrun/config/boss.json`）定义筛选条件；SQLite（`~/.geekgeekrun/storage/public.db`）存储记录 |

**关键特性**：
- **自动开聊**：根据配置的职位来源、筛选条件自动与 Boss 沟通
- **多维度筛选**：城市、薪资、经验、职位名称/类型/描述正则、BOSS 活跃度
- **反检测**：StealthPlugin + LaodengPlugin + UA 匿名化
- **已读不回复聊**：自动跟进已读不回的 Boss
- **出站 Webhook**：可将开聊结果 POST 到指定 URL

**局限性**：
- **无 headless 模式**：Puppeteer 硬编码为 `headless: false`，需要 GUI 环境（不能直接在纯服务器上运行）
- **无入站 REST API**：不能通过 HTTP 触发，只能通过 Electron CLI 或直接 `node` 启动
- **需手动登录**：首次使用需通过"BOSS 登录助手"扫码登录，Cookie 保存在本地
- **平台风控**：高频操作可能触发 Boss 直聘风控，需要"贤者时间"（自动暂停）机制

---

### 1.4 三者协作关系

```
┌─────────────────────────────────────────────────────────────────────┐
│                        n8n（调度中枢）                               │
│  · 定时触发（每 6 小时）                                              │
│  · 数据清洗 & 去重                                                   │
│  · 条件判断（分数 > 80）                                              │
│  · 调用外部服务                                                      │
│  · 记录日志到数据库                                                   │
└────────┬────────────────────────┬───────────────────────────────────┘
         │                        │
         ▼                        ▼
┌─────────────────┐    ┌──────────────────────────────────┐
│  career-ops      │    │  geekgeekrun                     │
│  （AI 评分引擎）  │    │  （Boss 直聘自动投递）             │
│                  │    │                                   │
│  · 接收 JD 文本  │    │  · 接收高匹配岗位列表               │
│  · AI 匹配评分   │    │  · 自动与 Boss 开聊                │
│  · 输出评估报告  │    │  · 记录投递结果                    │
└─────────────────┘    └──────────────────────────────────┘
```

**数据传递方式**：
1. **n8n → career-ops**：n8n 通过 HTTP Request 调用 career-ops 的评分 API（需封装为 Flask/FastAPI 服务）
2. **n8n → geekgeekrun**：方案 A：n8n 通过 SSH + CLI 命令触发 geekgeekrun；方案 B：n8n 将高匹配岗位信息通过 Webhook 推送给 geekgeekrun
3. **career-ops → n8n**：评分结果通过 HTTP 响应返回给 n8n
4. **geekgeekrun → n8n**：投递结果通过 geekgeekrun 的出站 Webhook 回传给 n8n

---

## 第二步：整体架构设计

### 2.1 数据流图

```
                        ┌───────────────────┐
                        │   阿里云服务器      │
                        │  Ubuntu 22.04      │
                        │                    │
  ┌────────────────────┼────────────────────┼────────────────────────────┐
  │                    │                    │                            │
  │   ┌────────────────▼────────────────┐   │                            │
  │   │       n8n (Docker)              │   │                            │
  │   │                                 │   │                            │
  │   │  ┌──────────────┐               │   │                            │
  │   │  │ Schedule     │ 每6小时触发   │   │                            │
  │   │  │ Trigger      │───────────┐   │   │                            │
  │   │  └──────────────┘           │   │   │                            │
  │   │                             ▼   │   │                            │
  │   │  ┌──────────────┐               │   │                            │
  │   │  │ HTTP Request │ 抓取Boss直聘  │   │                            │
  │   │  │ (搜索API)    │ 搜索结果      │   │                            │
  │   │  └──────┬───────┘               │   │                            │
  │   │         ▼                       │   │                            │
  │   │  ┌──────────────┐               │   │                            │
  │   │  │ Code (JS)    │ 数据清洗      │   │                            │
  │   │  │ 解析JSON     │ + 去重        │   │                            │
  │   │  └──────┬───────┘               │   │                            │
  │   │         ▼                       │   │                            │
  │   │  ┌──────────────┐               │   │                            │
  │   │  │ HTTP Request │ 调用career-ops│   │                            │
  │   │  │ (POST评分API)│ 评分服务      │   │                            │
  │   │  └──────┬───────┘               │   │                            │
  │   │         ▼                       │   │                            │
  │   │  ┌──────────────┐               │   │                            │
  │   │  │ IF 节点      │ 分数>80?      │   │                            │
  │   │  └──┬───────┬───┘               │   │                            │
  │   │     │       │                   │   │                            │
  │   │  YES│    NO │                   │   │                            │
  │   │     ▼       ▼                   │   │                            │
  │   │  ┌──────┐ ┌──────┐             │   │                            │
  │   │  │通知你│ │记录日│             │   │                            │
  │   │  │手动投│ │志跳过│             │   │                            │
  │   │  │递    │ │      │             │   │                            │
  │   │  └──────┘ └──────┘             │   │                            │
  │   └─────────────────────────────────┘   │                            │
  │                                         │                            │
  │   ┌─────────────────────────────────┐   │                            │
  │   │   career-ops 评分服务            │   │                            │
  │   │   (Flask/FastAPI 封装)           │   │                            │
  │   │                                 │   │                            │
  │   │  POST /evaluate                 │   │                            │
  │   │  { "jd": "...", "company": "..."│   │                            │
  │   │  }                              │   │                            │
  │   │         │                       │   │                            │
  │   │         ▼                       │   │                            │
  │   │  gemini-eval.mjs 调用           │   │                            │
  │   │  Gemini API 评分                │   │                            │
  │   │         │                       │   │                            │
  │   │         ▼                       │   │                            │
  │   │  { "score": 4.2, "report": "..."│   │                            │
  │   │  }                              │   │                            │
  │   └─────────────────────────────────┘   │                            │
  │                                         │                            │
  │   ┌─────────────────────────────────┐   │                            │
  │   │   SQLite 数据库                  │   │                            │
  │   │   (去重 + 历史记录)              │   │                            │
  │   └─────────────────────────────────┘   │                            │
  └─────────────────────────────────────────┘                            │
                                                                         │
  ┌──────────────────────────────────────────────────────────────────────┘
  │
  │   ┌─────────────────────────────────┐
  │   │   你的本地电脑 / 另一台有GUI的机器│
  │   │                                 │
  │   │   geekgeekrun (Electron)        │
  │   │                                 │
  │   │  方案A: 手动接收通知后运行       │
  │   │  方案B: 接收Webhook后自动运行    │
  │   │                                 │
  │   │  · 自动在Boss直聘开聊            │
  │   │  · 出站Webhook回报结果           │
  │   └─────────────────────────────────┘
  └─────────────────────────────────────────
```

### 2.2 外部服务依赖

| 外部服务 | 用途 | 费用 |
|---------|------|------|
| **Boss 直聘搜索接口** | 抓取岗位信息 | 免费（需处理反爬） |
| **Google Gemini API** | AI 评分（career-ops 底层） | 免费额度：15 RPM / 1M tokens/天 |
| **DingTalk/邮件** | 高匹配岗位通知 | 免费 |
| **Claude API（可选）** | 更高质量的 AI 评估 | 付费 |

### 2.3 模块职责划分

| 模块 | 技术栈 | 职责 | 部署位置 |
|------|--------|------|---------|
| **n8n** | Docker + Node.js | 定时调度、数据抓取、流程编排、条件判断、通知发送 | 阿里云服务器 |
| **career-ops 评分服务** | Python (Flask) + Node.js | JD 评分、匹配分析、报告生成 | 阿里云服务器 |
| **geekgeekrun** | Electron + Puppeteer | Boss 直聘自动开聊 | 本地电脑（需 GUI） |
| **SQLite/PostgreSQL** | 数据库 | 存储岗位 ID、评分、投递历史（防重复） | 阿里云服务器 |

---

## 第三步：详细整合实现步骤

### 3.1 环境准备

#### 阿里云服务器（Ubuntu 22.04）

```bash
# 1. 更新系统
sudo apt update && sudo apt upgrade -y

# 2. 安装 Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER
# 重新登录使 docker 组生效

# 3. 安装 Docker Compose
sudo apt install docker-compose-plugin -y

# 4. 安装 Node.js 22（用于 career-ops）
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt install -y nodejs

# 5. 安装 Python 3 + pip（用于 Flask 评分服务）
sudo apt install -y python3 python3-pip python3-venv

# 6. 安装 Git
sudo apt install -y git

# 7. 创建项目目录
mkdir -p ~/job-automation/{n8n,career-ops,scoring-service,data}
```

#### 本地电脑（Windows）

```bash
# 1. 下载 geekgeekrun 发行版
# 访问 https://github.com/geekgeekrun/geekgeekrun/releases
# 下载 .exe 安装包并安装

# 2. 安装完成后启动，按引导完成配置：
#    - 登录 Boss 直聘
#    - 配置求职偏好（城市、薪资、经验等）
#    - 配置职位关键词（嵌入式、Linux 开发等）
```

---

### 3.2 部署 n8n

#### Docker Compose 配置

创建 `~/job-automation/n8n/docker-compose.yml`：

```yaml
version: '3.8'

services:
  n8n:
    image: docker.n8n.io/n8nio/n8n
    container_name: n8n
    restart: unless-stopped
    ports:
      - "5678:5678"
    environment:
      - N8N_DIAGNOSTICS_ENABLED=false
      - N8N_HIRING_BANNER_ENABLED=false
      - N8N_USER_FOLDER=/home/node/.n8n
      - GENERIC_TIMEZONE=Asia/Shanghai
      - TZ=Asia/Shanghai
      - N8N_PAYLOAD_SIZE_MAX=50
      - WEBHOOK_URL=http://你的服务器IP:5678/
    volumes:
      - n8n_data:/home/node/.n8n
    networks:
      - job-net

  postgres:
    image: postgres:16
    container_name: job-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: jobadmin
      POSTGRES_PASSWORD: 你的安全密码
      POSTGRES_DB: job_automation
    volumes:
      - pg_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - job-net

volumes:
  n8n_data:
  pg_data:

networks:
  job-net:
    driver: bridge
```

启动服务：

```bash
cd ~/job-automation/n8n
docker compose up -d

# 查看日志
docker compose logs -f n8n
```

访问 `http://你的服务器IP:5678`，完成 n8n 初始化设置（创建管理员账号）。

#### 生成 API Key

在 n8n UI 中：Settings → API → Create API Key，保存好 Key。

---

### 3.3 配置 career-ops 及封装评分服务

#### 3.3.1 安装 career-ops

```bash
cd ~/job-automation/career-ops
git clone https://github.com/santifer/career-ops.git .
npm install
npx playwright install chromium
```

#### 3.3.2 配置环境变量

```bash
cp .env.example .env
```

编辑 `.env`：

```env
GEMINI_API_KEY=你的Gemini_API_Key
GEMINI_MODEL=gemini-2.5-flash
```

获取免费 API Key：https://aistudio.google.com/apikey

#### 3.3.3 创建简历和配置文件

```bash
# 创建简历（必须）
# 将你的简历内容写入 cv.md

# 创建配置文件
cp config/profile.example.yml config/profile.yml
# 编辑 config/profile.yml，填入你的个人信息

# 创建门户配置
cp templates/portals.example.yml portals.yml
# 编辑 portals.yml，配置你想搜索的公司和关键词

# 创建用户自定义文件
cp modes/_profile.template.md modes/_profile.md
# 编辑 modes/_profile.md，定制你的求职偏好

# 创建追踪器
mkdir -p data
cat > data/applications.md << 'EOF'
# Applications Tracker

| # | Date | Company | Role | Score | Status | PDF | Report | Notes |
|---|------|---------|------|-------|--------|-----|--------|-------|
EOF
```

#### 3.3.4 测试命令行评分

```bash
# 测试 Gemini 评分
node gemini-eval.mjs "我们正在寻找一名嵌入式Linux开发工程师，要求：
1. 3年以上嵌入式Linux开发经验
2. 熟悉ARM架构和BSP开发
3. 精通C/C++语言
4. 有驱动开发经验优先
薪资：20-35K"
```

#### 3.3.5 封装为 Flask HTTP 服务

创建 `~/job-automation/scoring-service/app.py`：

```python
import subprocess
import json
import re
import os
from flask import Flask, request, jsonify

app = Flask(__name__)

CAREER_OPS_DIR = os.environ.get('CAREER_OPS_DIR', '/root/job-automation/career-ops')

def parse_score_from_output(output):
    score_match = re.search(r'---SCORE_SUMMARY---\s*\n(.*?)\n---END_SCORE_SUMMARY---', output, re.DOTALL)
    if score_match:
        summary = score_match.group(1)
        result = {}
        for line in summary.strip().split('\n'):
            if ':' in line:
                key, value = line.split(':', 1)
                result[key.strip()] = value.strip()
        return result
    score_line = re.search(r'(?:Score|SCORE|分数|评分)[：:\s]*(\d+\.?\d*)', output)
    if score_line:
        return {'SCORE': score_line.group(1)}
    return None

@app.route('/health', methods=['GET'])
def health():
    return jsonify({'status': 'ok'})

@app.route('/evaluate', methods=['POST'])
def evaluate():
    data = request.get_json()
    if not data or 'jd' not in data:
        return jsonify({'error': 'Missing "jd" field'}), 400

    jd_text = data['jd']
    company = data.get('company', 'Unknown')
    role = data.get('role', 'Unknown')

    try:
        result = subprocess.run(
            ['node', 'gemini-eval.mjs', '--no-save', jd_text],
            capture_output=True,
            text=True,
            cwd=CAREER_OPS_DIR,
            timeout=120
        )

        output = result.stdout + result.stderr
        score_info = parse_score_from_output(output)

        if score_info:
            score_raw = float(score_info.get('SCORE', '0'))
            if score_raw <= 5:
                score_100 = round(score_raw * 20, 1)
            else:
                score_100 = score_raw
        else:
            score_100 = 0

        return jsonify({
            'company': company,
            'role': role,
            'score': score_100,
            'score_raw': score_info.get('SCORE', 'N/A') if score_info else 'N/A',
            'archetype': score_info.get('ARCHETYPE', '') if score_info else '',
            'legitimacy': score_info.get('LEGITIMACY', '') if score_info else '',
            'report': output,
            'success': True
        })

    except subprocess.TimeoutExpired:
        return jsonify({'error': 'Evaluation timed out', 'success': False}), 504
    except Exception as e:
        return jsonify({'error': str(e), 'success': False}), 500

@app.route('/evaluate-batch', methods=['POST'])
def evaluate_batch():
    data = request.get_json()
    if not data or 'jobs' not in data:
        return jsonify({'error': 'Missing "jobs" field'}), 400

    results = []
    for job in data['jobs']:
        jd = job.get('jd', '')
        if not jd:
            results.append({'error': 'Empty JD', 'success': False})
            continue

        try:
            result = subprocess.run(
                ['node', 'gemini-eval.mjs', '--no-save', jd],
                capture_output=True,
                text=True,
                cwd=CAREER_OPS_DIR,
                timeout=120
            )
            output = result.stdout + result.stderr
            score_info = parse_score_from_output(output)

            if score_info:
                score_raw = float(score_info.get('SCORE', '0'))
                score_100 = round(score_raw * 20, 1) if score_raw <= 5 else score_raw
            else:
                score_100 = 0

            results.append({
                'company': job.get('company', 'Unknown'),
                'role': job.get('role', 'Unknown'),
                'score': score_100,
                'success': True
            })
        except Exception as e:
            results.append({'error': str(e), 'success': False})

    return jsonify({'results': results})

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000, debug=False)
```

创建 `~/job-automation/scoring-service/requirements.txt`：

```
flask==3.1.1
gunicorn==23.0.0
```

#### 3.3.6 用 systemd 管理评分服务

创建 `/etc/systemd/system/scoring-service.service`：

```ini
[Unit]
Description=Career-Ops Scoring Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/job-automation/scoring-service
Environment=CAREER_OPS_DIR=/root/job-automation/career-ops
ExecStart=/usr/bin/python3 -m gunicorn -w 2 -b 0.0.0.0:5000 app:app
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable scoring-service
sudo systemctl start scoring-service

# 测试
curl -X POST http://localhost:5000/evaluate \
  -H "Content-Type: application/json" \
  -d '{"jd": "嵌入式Linux开发工程师，要求3年以上经验...", "company": "测试公司", "role": "嵌入式工程师"}'
```

---

### 3.4 数据存储设计

在 PostgreSQL 中创建岗位数据库表：

```sql
-- 连接到 job_automation 数据库
-- docker exec -it job-postgres psql -U jobadmin -d job_automation

CREATE TABLE IF NOT EXISTS jobs (
    id SERIAL PRIMARY KEY,
    job_id VARCHAR(100) UNIQUE NOT NULL,
    source VARCHAR(50) NOT NULL,
    company VARCHAR(200) NOT NULL,
    role VARCHAR(300) NOT NULL,
    location VARCHAR(100),
    salary VARCHAR(100),
    jd_text TEXT,
    url VARCHAR(500),
    score DECIMAL(5,2),
    score_detail JSONB,
    status VARCHAR(50) DEFAULT 'pending',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    scored_at TIMESTAMP,
    submitted_at TIMESTAMP,
    notes TEXT
);

CREATE INDEX idx_jobs_status ON jobs(status);
CREATE INDEX idx_jobs_score ON jobs(score);
CREATE INDEX idx_jobs_job_id ON jobs(job_id);

CREATE TABLE IF NOT EXISTS job_logs (
    id SERIAL PRIMARY KEY,
    job_id VARCHAR(100) REFERENCES jobs(job_id),
    action VARCHAR(50) NOT NULL,
    detail TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

### 3.5 集成 geekgeekrun

#### 方案选择

考虑到 geekgeekrun 的限制（无 headless 模式、无入站 API），推荐**渐进式方案**：

**Phase 1（推荐起步）：通知 + 手动投递**
- n8n 筛选出高匹配岗位后，通过钉钉/邮件通知你
- 你在本地手动打开 geekgeekrun 或直接在 Boss 直聘上沟通

**Phase 2（进阶）：半自动投递**
- n8n 将高匹配岗位的公司名和职位名写入一个共享列表
- 你定期将该列表导入到 geekgeekrun 的配置中
- geekgeekrun 根据配置自动筛选并开聊

**Phase 3（高级）：远程触发 + 出站 Webhook**
- geekgeekrun 配置出站 Webhook，将投递结果发送回 n8n
- n8n 记录完整的投递历史

#### geekgeekrun 配置建议

编辑 `~/.geekgeekrun/config/boss.json`，配置适合嵌入式/Linux 开发的筛选条件：

```json
{
  "jobSourceList": [
    {
      "type": "search",
      "keywords": ["嵌入式开发", "Linux驱动开发", "BSP工程师", "嵌入式Linux"]
    },
    {
      "type": "expect",
      "keywords": ["嵌入式软件工程师", "Linux内核开发"]
    }
  ],
  "expectCityList": ["深圳", "上海", "北京"],
  "expectJobRegExpStr": "嵌入式|Linux|BSP|驱动",
  "blockCompanyNameRegExpStr": "外包|人力",
  "isSageTimeEnabled": true,
  "sageTimeOpTimes": 30,
  "sageTimePauseMinute": 10
}
```

---

### 3.6 通知配置（钉钉 Webhook）

创建钉钉自定义机器人，获取 Webhook URL。

在 n8n 工作流中，使用 HTTP Request 节点发送通知：

```json
{
  "msgtype": "markdown",
  "markdown": {
    "title": "🎯 高匹配岗位推荐",
    "text": "## 🎯 发现高匹配岗位\n\n- **公司**：{company}\n- **职位**：{role}\n- **评分**：{score}/100\n- **地点**：{location}\n- **薪资**：{salary}\n\n[查看详情]({url})\n\n---\n请及时查看并决定是否投递！"
  }
}
```

---

## 第四步：关键代码/配置示例

### 4.1 n8n 工作流 JSON 定义

```json
{
  "name": "求职自动化 - 岗位抓取与评分",
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "hours",
              "hoursInterval": 6
            }
          ]
        }
      },
      "id": "trigger-1",
      "name": "每6小时触发",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [250, 300]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://www.zhipin.com/wapi/zpgeek/search/joblist.json",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "User-Agent",
              "value": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36"
            },
            {
              "name": "Cookie",
              "value": "你的Boss直聘Cookie"
            }
          ]
        },
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            { "name": "scene", "value": "1" },
            { "name": "query", "value": "嵌入式Linux开发" },
            { "name": "city", "value": "101280600" },
            { "name": "experience", "value": "104,105,106" },
            { "name": "payType", "value": "" },
            { "name": "partTime", "value": "" },
            { "name": "degree", "value": "" },
            { "name": "industry", "value": "" },
            { "name": "scale", "value": "" },
            { "name": "stage", "value": "" },
            { "name": "position", "value": "" },
            { "name": "salary", "value": "" },
            { "name": "multiBusinessDistrict", "value": "" },
            { "name": "multiSubway", "value": "" },
            { "name": "page", "value": "1" },
            { "name": "pageSize", "value": "30" }
          ]
        },
        "options": {
          "response": {
            "response": {
              "responseFormat": "json"
            }
          }
        }
      },
      "id": "fetch-jobs",
      "name": "抓取Boss直聘岗位",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [470, 300]
    },
    {
      "parameters": {
        "jsCode": "// 解析Boss直聘搜索结果\nconst inputData = $input.all();\nconst jobs = [];\n\nfor (const item of inputData) {\n  const jobList = item.json?.zpData?.jobList || [];\n  for (const job of jobList) {\n    jobs.push({\n      json: {\n        jobId: job.encryptJobId || job.jobId,\n        company: job.brandName || job.companyName,\n        role: job.jobName,\n        location: job.areaDistrict,\n        salary: job.salaryDesc,\n        experience: job.jobExperience,\n        education: job.jobDegree,\n        jd: job.skills ? job.skills.join(', ') : '',\n        url: `https://www.zhipin.com/job_detail/${job.encryptJobId}.html`,\n        bossTitle: job.bossTitle,\n        bossOnline: job.bossOnline\n      }\n    });\n  }\n}\n\nreturn jobs;"
      },
      "id": "parse-jobs",
      "name": "解析岗位数据",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [690, 300]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "http://localhost:5000/evaluate",
        "sendBody": true,
        "bodyParameters": {
          "parameters": [
            {
              "name": "jd",
              "value": "={{ $json.jd }}"
            },
            {
              "name": "company",
              "value": "={{ $json.company }}"
            },
            {
              "name": "role",
              "value": "={{ $json.role }}"
            }
          ]
        },
        "options": {
          "timeout": 180000
        }
      },
      "id": "score-job",
      "name": "AI评分",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [910, 300]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict"
          },
          "conditions": [
            {
              "id": "condition-1",
              "leftValue": "={{ $json.score }}",
              "rightValue": "80",
              "operator": {
                "type": "number",
                "operation": "gte"
              }
            }
          ],
          "combinator": "and"
        }
      },
      "id": "check-score",
      "name": "分数>=80?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [1130, 300]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://oapi.dingtalk.com/robot/send?access_token=你的钉钉Token",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={\n  \"msgtype\": \"markdown\",\n  \"markdown\": {\n    \"title\": \"🎯 高匹配岗位\",\n    \"text\": \"## 🎯 发现高匹配岗位\\n\\n- **公司**：{{ $json.company }}\\n- **职位**：{{ $json.role }}\\n- **评分**：{{ $json.score }}/100\\n\\n请及时查看并投递！\"\n  }\n}"
      },
      "id": "notify",
      "name": "钉钉通知",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [1350, 200]
    },
    {
      "parameters": {
        "jsCode": "// 记录到数据库（通过HTTP调用）\nconst job = $input.all()[0].json;\nreturn [{\n  json: {\n    ...job,\n    status: job.score >= 80 ? 'notified' : 'skipped',\n    timestamp: new Date().toISOString()\n  }\n}];"
      },
      "id": "log-result",
      "name": "记录结果",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1350, 400]
    }
  ],
  "connections": {
    "每6小时触发": {
      "main": [
        [{ "node": "抓取Boss直聘岗位", "type": "main", "index": 0 }]
      ]
    },
    "抓取Boss直聘岗位": {
      "main": [
        [{ "node": "解析岗位数据", "type": "main", "index": 0 }]
      ]
    },
    "解析岗位数据": {
      "main": [
        [{ "node": "AI评分", "type": "main", "index": 0 }]
      ]
    },
    "AI评分": {
      "main": [
        [{ "node": "分数>=80?", "type": "main", "index": 0 }]
      ]
    },
    "分数>=80?": {
      "main": [
        [{ "node": "钉钉通知", "type": "main", "index": 0 }],
        [{ "node": "记录结果", "type": "main", "index": 0 }]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  }
}
```

### 4.2 Python 评分脚本（独立调用）

```python
#!/usr/bin/env python3
"""独立的 career-ops 评分调用脚本"""

import subprocess
import sys
import json
import re

CAREER_OPS_DIR = "/root/job-automation/career-ops"

def evaluate_job(jd_text, company="Unknown", role="Unknown"):
    try:
        result = subprocess.run(
            ['node', 'gemini-eval.mjs', '--no-save', jd_text],
            capture_output=True,
            text=True,
            cwd=CAREER_OPS_DIR,
            timeout=120
        )
        output = result.stdout
        score_match = re.search(r'(?:Score|SCORE)[：:\s]*(\d+\.?\d*)', output)
        score = float(score_match.group(1)) if score_match else 0
        if score <= 5:
            score = round(score * 20, 1)

        return {
            "company": company,
            "role": role,
            "score": score,
            "output": output[:500],
            "success": True
        }
    except Exception as e:
        return {"error": str(e), "success": False}

if __name__ == "__main__":
    if len(sys.argv) < 2:
        print("Usage: python3 score_job.py <jd_text> [company] [role]")
        sys.exit(1)

    jd = sys.argv[1]
    company = sys.argv[2] if len(sys.argv) > 2 else "Unknown"
    role = sys.argv[3] if len(sys.argv) > 3 else "Unknown"

    result = evaluate_job(jd, company, role)
    print(json.dumps(result, ensure_ascii=False, indent=2))
```

### 4.3 整合测试 Shell 脚本

```bash
#!/bin/bash
# test_pipeline.sh — 手动测试整个流程

set -e

SCORING_URL="http://localhost:5000"
N8N_URL="http://localhost:5678"

echo "=== Step 1: 检查服务状态 ==="
echo "检查评分服务..."
curl -s "$SCORING_URL/health" | jq .
echo ""

echo "检查 n8n..."
curl -s "$N8N_URL/healthz" | head -c 100
echo ""
echo ""

echo "=== Step 2: 测试评分服务 ==="
TEST_JD="我们正在寻找一名嵌入式Linux开发工程师：
1. 3年以上嵌入式Linux开发经验
2. 熟悉ARM架构和BSP开发
3. 精通C/C++语言
4. 有驱动开发经验优先
5. 熟悉Yocto/Buildroot构建系统
薪资范围：20-35K"

echo "发送测试JD到评分服务..."
RESULT=$(curl -s -X POST "$SCORING_URL/evaluate" \
  -H "Content-Type: application/json" \
  -d "{\"jd\": \"$TEST_JD\", \"company\": \"测试科技\", \"role\": \"嵌入式Linux工程师\"}")

echo "评分结果："
echo "$RESULT" | jq .
echo ""

SCORE=$(echo "$RESULT" | jq -r '.score')
echo "评分: $SCORE"

if (( $(echo "$SCORE >= 80" | bc -l) )); then
    echo "✅ 分数 >= 80，应该投递"
else
    echo "❌ 分数 < 80，跳过"
fi
echo ""

echo "=== Step 3: 检查数据库 ==="
docker exec job-postgres psql -U jobadmin -d job_automation -c "SELECT COUNT(*) as total_jobs FROM jobs;" 2>/dev/null || echo "数据库表尚未创建"
echo ""

echo "=== 测试完成 ==="
```

---

## 第五步：渐进式开发计划

### Phase 1：基础架构搭建（1-2 天）

**目标**：在阿里云服务器上部署 n8n，实现手动触发抓取 Boss 直聘岗位

**任务清单**：
- [ ] 阿里云服务器安装 Docker + Docker Compose
- [ ] 创建 `docker-compose.yml`，部署 n8n + PostgreSQL
- [ ] 完成 n8n 初始化，创建管理员账号，生成 API Key
- [ ] 创建第一个工作流：Schedule Trigger → HTTP Request（Boss 直聘搜索 API） → Code（解析 JSON） → 输出到控制台
- [ ] 处理 Boss 直聘的 Cookie 认证和反爬机制
- [ ] 测试手动执行和定时执行

**验证标准**：n8n 能定时抓取 Boss 直聘的岗位列表，输出结构化 JSON 数据

**Boss 直聘搜索接口说明**：
- 搜索接口：`https://www.zhipin.com/wapi/zpgeek/search/joblist.json`
- 需要有效的登录 Cookie
- 城市代码：101010100=北京，101020100=上海，101280600=深圳
- 经验要求：104=1-3年，105=3-5年，106=5-10年
- **注意**：Cookie 有效期有限，需要定期更新；建议使用 geekgeekrun 的登录助手获取 Cookie

---

### Phase 2：AI 评分独立运行（1-2 天）

**目标**：在服务器上独立运行 career-ops 评分脚本，对测试 JD 打分

**任务清单**：
- [ ] 安装 Node.js 22 + npm 依赖
- [ ] 配置 Gemini API Key（免费）
- [ ] 创建 `cv.md`（你的简历）
- [ ] 创建 `config/profile.yml`（你的个人信息）
- [ ] 创建 `modes/_profile.md`（定制嵌入式/Linux 开发的评估 prompt）
- [ ] 安装 Playwright + Chromium
- [ ] 测试 `node gemini-eval.mjs "测试JD文本"`
- [ ] 调整评分 prompt，使其更适合嵌入式/Linux 岗位

**验证标准**：能对一条 JD 输出评分和评估报告

**定制评分 prompt 的建议**：
- 在 `modes/_profile.md` 中定义嵌入式/Linux 开发的原型（Archetype）
- 调整评分权重，使技术栈匹配度权重更高
- 添加嵌入式领域的关键技能：ARM、BSP、Device Tree、Yocto、RTOS、C/C++、驱动开发等

---

### Phase 3：串联 n8n + career-ops（2-3 天）

**目标**：n8n 抓取岗位后自动调用 career-ops 评分，筛选高分岗位

**任务清单**：
- [ ] 创建 Flask 评分服务（`app.py`）
- [ ] 用 systemd 管理评分服务
- [ ] 测试 `POST /evaluate` 接口
- [ ] 在 n8n 工作流中添加 HTTP Request 节点，调用评分服务
- [ ] 添加 IF 节点，判断分数 >= 80
- [ ] 连接 PostgreSQL，存储岗位数据和评分结果
- [ ] 添加去重逻辑（已评分的岗位不重复评分）
- [ ] 端到端测试：定时触发 → 抓取 → 评分 → 判断 → 记录

**验证标准**：每 6 小时自动抓取并评分，高分岗位记录到数据库

---

### Phase 4：通知 + 手动投递（1-2 天）

**目标**：高匹配岗位自动通知你，你手动决定是否投递

**任务清单**：
- [ ] 创建钉钉自定义机器人，获取 Webhook URL
- [ ] 在 n8n 工作流中添加钉钉通知节点
- [ ] 设计通知消息格式（公司、职位、评分、薪资、链接）
- [ ] 添加邮件通知（可选，使用 n8n 的 Email 节点）
- [ ] 创建简单的历史查看页面（可选，用 n8n 的 webhook 返回岗位列表）
- [ ] 你手动在 Boss 直聘或 geekgeekrun 上投递高匹配岗位

**验证标准**：发现高匹配岗位时，钉钉/邮件实时通知你

**钉钉通知效果示例**：
```
🎯 发现高匹配岗位

- 公司：华为技术
- 职位：嵌入式Linux开发工程师
- 评分：88/100
- 地点：深圳
- 薪资：25-40K·15薪
- 经验：3-5年

请及时查看并投递！
```

---

### Phase 5：集成 geekgeekrun + 全自动化（可选，3-5 天）

**目标**：研究 geekgeekrun 的自动化调用，实现半自动/全自动投递

**任务清单**：
- [ ] 在本地电脑安装 geekgeekrun
- [ ] 配置 geekgeekrun 的求职偏好（城市、薪资、关键词）
- [ ] 测试 `--mode=geekAutoStartWithBoss` 无窗口运行
- [ ] 配置 geekgeekrun 的出站 Webhook，将投递结果发送回 n8n
- [ ] 在 n8n 中创建 Webhook 节点接收投递结果
- [ ] 设计投递频率控制（避免触发 Boss 直聘风控）
- [ ] 完善投递历史追踪和统计

**风控注意事项**：
- 每天开聊不超过 50 次
- 启用"贤者时间"（每 30 次操作暂停 10 分钟）
- 建议注册专用 Boss 直聘账号
- 避免在公司网络/设备上使用
- 定期检查账号状态

**geekgeekrun 出站 Webhook 配置**：
- URL：`http://你的服务器IP:5678/webhook/boss-result`
- Method：POST
- Send Mode：realtime
- Content Type：application/json
- Payload Options：includeBasicInfo = true

---

### 时间线总结

| 阶段 | 内容 | 预计时间 | 产出 |
|------|------|---------|------|
| Phase 1 | 部署 n8n + 抓取岗位 | 1-2 天 | 定时抓取 Boss 直聘岗位 |
| Phase 2 | 配置 career-ops 评分 | 1-2 天 | JD 评分脚本可用 |
| Phase 3 | 串联 n8n + 评分服务 | 2-3 天 | 自动抓取 + 评分 + 筛选 |
| Phase 4 | 通知 + 手动投递 | 1-2 天 | 高匹配岗位实时通知 |
| Phase 5 | geekgeekrun 自动投递 | 3-5 天 | 全自动/半自动投递（可选） |

**总计**：约 8-14 天完成全部功能，Phase 1-4 约 5-9 天即可投入使用。

---

## 附录

### A. Boss 直聘城市代码速查

| 城市 | 代码 |
|------|------|
| 北京 | 101010100 |
| 上海 | 101020100 |
| 广州 | 101280100 |
| 深圳 | 101280600 |
| 杭州 | 101210100 |
| 成都 | 101270100 |
| 武汉 | 101200100 |
| 南京 | 101190100 |
| 苏州 | 101190400 |
| 西安 | 101110100 |

### B. 常用 n8n 环境变量

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `N8N_PORT` | n8n 端口 | 5678 |
| `N8N_PROTOCOL` | http/https | http |
| `WEBHOOK_URL` | Webhook 基础 URL | - |
| `GENERIC_TIMEZONE` | 时区 | America/New_York |
| `N8N_PAYLOAD_SIZE_MAX` | 最大请求体(MB) | 16 |
| `DB_TYPE` | 数据库类型 | sqlite |
| `DB_POSTGRESDB_HOST` | PostgreSQL 主机 | localhost |
| `DB_POSTGRESDB_PORT` | PostgreSQL 端口 | 5432 |
| `DB_POSTGRESDB_DATABASE` | 数据库名 | n8n |
| `DB_POSTGRESDB_USER` | 用户名 | postgres |
| `DB_POSTGRESDB_PASSWORD` | 密码 | - |

### C. 故障排查

| 问题 | 排查方法 |
|------|---------|
| n8n 无法启动 | `docker compose logs n8n` 查看日志 |
| 评分服务超时 | 检查 Gemini API Key 是否有效，网络是否通畅 |
| Boss 直聘抓取失败 | Cookie 是否过期，是否被反爬 |
| geekgeekrun 无法开聊 | 是否已登录，是否触发风控 |
| PostgreSQL 连接失败 | 检查端口映射和防火墙设置 |
